# Git Hands-on

All Git hands-on exercises (repo setup, .gitignore, branching, merging,
conflict resolution, pull/push) were completed in a separate GitLab
repository, as required by the exercise instructions.

**GitLab repo:**https://gitlab.com/cognizant_git/GitDemo

The full commit history (branches, merge commits, conflict resolution) is
visible in the repo's Graph/Network view and commit log.
